# v0.6.0 manual smoke record

date: 2026-09-01
binary_sha256: f1baa8908609c5cbf0ab7aadf33afe1faf2fe18ed9e79cef4c23d57abaca94f1
source: e3990492c Merge pull request #209 from glebis/release/v0.6.0
installed_by: scripts/install-local-build.sh (landing worktree build)

## What was verified

- App launches from /Applications/Cull.app; version reports 0.6.0.
- Library/grid renders with thumbnails; no regressions observed in daily use.
- Ratings and selections behave normally.
- Sidebar sections (recency rail, collections, clipboard monitor chip) behave.
- MCP/agent surface reachable (headless `--mcp-stdio` instances were re-spawned
  after install and reconnected).
- Icon variant picker available in settings.

## Known non-blockers

- The in-progress Lineage thumbnail zoom (`lineageImageScale`) is unpushed WIP
  on `wip/pre-release-2026-09-01` and intentionally not part of this release.
  Human verdict: pass — no build regressions found; the missing zoom is
  unlanded work, not a 0.6.0 regression.