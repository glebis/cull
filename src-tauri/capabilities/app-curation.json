{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app-curation",
  "description": "Library curation IPC for ratings, collections, lineage, sessions, canvases, and undo.",
  "windows": ["main", "window-*"],
  "permissions": ["app-curation"]
}
