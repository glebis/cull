{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app-photos",
  "description": "Apple Photos authorization, read-only catalog metadata, and local-only preview IPC.",
  "windows": ["main"],
  "permissions": ["app-photos"]
}
